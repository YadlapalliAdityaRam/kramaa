import React from 'react';

const LearningPath = () => {
    return (
        <div style={{ padding: '2rem' }}>
            <h1>Learning Paths</h1>
            <p>Guided paths to master Data Structures and Algorithms.</p>
        </div>
    );
};

export default LearningPath;
