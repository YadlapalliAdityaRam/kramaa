import React from 'react';

const Visualize = () => {
    return (
        <div style={{ padding: '2rem' }}>
            <h1>Algorithm Visualizer Hub</h1>
            <p>Select an algorithm to start visualizing!</p>
        </div>
    );
};

export default Visualize;
